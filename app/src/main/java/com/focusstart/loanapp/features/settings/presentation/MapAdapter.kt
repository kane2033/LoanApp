package com.focusstart.loanapp.features.settings.presentation

/*
class MapAdapter<K, V>(private val map: Map<K, V>): BaseAdapter() {

    override fun getCount() = map.size

    override fun getItem(position: Int): Any {
        return map.get(position)
    }

    override fun getItemId(position: Int) = 0L

    override fun getView(position: Int, convertView: View?, parent: ViewGroup?): View {
        val label = getView(position, convertView, parent) as TextView
        label.text = map

        return label
    }
}*/
